import React from 'react';
import PropTypes from 'prop-types'
import {
  View,
  Text,
  Image,
  ScrollView,
SafeAreaView,
  FlatList,
  SectionList,
  TextInput,
  TouchableOpacity,
  Pressable,
} from 'react-native';

const AboutIelts = ({navigation}) => {
  return (
    <View className=''>
      
</View>
  );
};

AboutIelts.propTypes = {
};
export default AboutIelts;