import React from 'react';
import PropTypes from 'prop-types'
import {
    View,
    Text,
    Image,
    ScrollView,
    SafeAreaView,
    FlatList,
    SectionList,
    TextInput,
    TouchableOpacity,
    Pressable,
} from 'react-native';

const Policy = () => {
    return (
        <View className=''>
           <Text className="">
           this is Policy screen
           </Text>
        </View>
    );
};

Policy.propTypes = {
};
export default Policy;