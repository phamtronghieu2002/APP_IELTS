import React from 'react';
import PropTypes from 'prop-types'
import {
    View,
    Text,
    Image,
    ScrollView,
    SafeAreaView,
    FlatList,
    SectionList,
    TextInput,
    TouchableOpacity,
    Pressable,
} from 'react-native';

const Term = () => {
    return (
        <View className='p-5'>
            <Text className="">
                this is Policy screen

            </Text>
        </View>
    );
};

Term.propTypes = {
};
export default Term;