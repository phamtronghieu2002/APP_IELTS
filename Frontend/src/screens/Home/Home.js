import React from 'react';
import PropTypes from 'prop-types'
import {
  View,
  Text,
  Image,
  ScrollView,
SafeAreaView,
  FlatList,
  SectionList,
  TextInput,
  TouchableOpacity,
  Pressable,
} from 'react-native';
import Item from '../../components/Item/Item';

const Home = () => {
  return (
    <View className=''>
      <Item 
      
      
       />
</View>
  );
};

Home.propTypes = {
};
export default Home;